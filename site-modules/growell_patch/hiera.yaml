---
version: 5

defaults:  # Used for any hierarchy level that omits these keys.
  datadir: data         # This path is relative to hiera.yaml's directory.
  data_hash: yaml_data  # Use the built-in YAML backend.

hierarchy:
#  - name: 'patching'
#    paths:
#      - "nodes/%{trusted.certname}.yaml"
#      - "%{cust.group}/%{cust.env}.yaml"
#      - "%{cust.group}.yaml"
#      - "%{kernel}.yaml"
  - name: 'proposed patching'
    paths:
      - "nodes/%{trusted.certname}.yaml"
      - "environments/%{server_facts.environment}/%{az_metadata.compute.zone}.yaml"
      - "environments/%{server_facts.environment}/0.yaml"
      - "environments/%{server_facts.environment}/%{os.name}.yaml"
      - "environments/%{server_facts.environment}.yaml"
